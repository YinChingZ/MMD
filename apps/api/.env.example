PORT=3000
DATABASE_URL=postgres://mmd:mmd@localhost:5432/mmd

# Values for the apiKeyEnvVar names referenced in models.config.json, e.g.:
# OPENAI_API_KEY=sk-...

# Encrypts BYOK API keys that users opt into saving (workspace_api_keys
# table). Generate with: openssl rand -base64 32
ENCRYPTION_KEY=
