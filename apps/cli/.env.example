# Copy to apps/cli/.env (gitignored) and set real values.
# The variable names here must match the "apiKeyEnvVar" fields in your
# models.config.json — these are examples, rename/add as needed.
MODEL_A_API_KEY=
MODEL_B_API_KEY=
MODEL_C_API_KEY=
