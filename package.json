{
  "name": "mmd",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspace @mmd/protocol && npm run build --workspace @mmd/model-adapters && npm run build --workspace @mmd/prompts && npm run build --workspace @mmd/cli"
  }
}
